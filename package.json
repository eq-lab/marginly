{
  "name": "marginly",
  "version": "0.1.0",
  "description": "Marginly protocol",
  "author": "EqLab",
  "private": true,
  "scripts": {
    "build": "yarn workspace @marginly/contracts install && yarn workspace @marginly/contracts compile && yarn workspace @marginly/deploy install && yarn workspace @marginly/deploy build && yarn workspace @marginly/cli install && yarn workspace @marginly/cli build && yarn workspace @marginly/int-tests install && yarn workspace @marginly/int-tests compile && yarn workspace @marginly/int-tests build && yarn workspace @marginly/frontend install && yarn workspace @marginly/frontend build && yarn workspace @marginly/keeper-contracts install && yarn workspace @marginly/keeper-contracts compile"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.48.1",
    "@typescript-eslint/parser": "^5.48.1",
    "eslint": "^8.31.0",
    "eslint-config-prettier": "^8.6.0",
    "prettier": "^2.8.2",
    "prettier-plugin-solidity": "1.0.0",
    "typescript": "^4.9.4"
  }
}
